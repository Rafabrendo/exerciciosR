#Listas list()

lista1 <- list("a", 1L, 1.5, TRUE)
lista1
typeof(lista1)

lista2 <- list(list(list(1 , 3, 5)))
lista2

str(lista2)
str(lista1)

#Nomeando os elementos de uma lista
lista_nomeada <- list('Chicago' = 1, 'New York' = 2, 'Los Angeles' = 3)
lista_nomeada
